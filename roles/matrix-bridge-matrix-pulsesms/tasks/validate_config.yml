---

- name: Fail if required settings not defined
  fail:
    msg: >-
      You need to define a required configuration setting (`{{ item }}`).
  when: "vars[item] == ''"
  with_items:
    - "matrix_matrix_pulsesms_appservice_token"
    - "matrix_matrix_pulsesms_homeserver_token"

